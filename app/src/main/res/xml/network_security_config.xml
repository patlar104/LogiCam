<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production configuration - enforce HTTPS -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system certificate authorities -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    
    <!-- Debug configuration - allow localhost for development -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <!-- Allow user-installed certificates in debug builds -->
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
    
    <!-- Domain-specific configurations (add as needed) -->
    <!-- Example for a specific API domain:
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.logicam.com</domain>
        <pin-set expiration="2026-01-01">
            <pin digest="SHA-256">base64-encoded-pin</pin>
        </pin-set>
    </domain-config>
    -->
</network-security-config>
